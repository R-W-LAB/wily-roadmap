Run the `wily-decompose-stage` skill for explicit Stage decomposition.

This command is approval-first. It must not run automatically from init or start.

Board reflection contract: after local `.wily` topology changes, emit or replay the `stage_decomposed_local` draft with `wily.py board sync-local <stage-id>` when configured, record deterministic evidence, and use actual-site visual verification only for failures, mismatches, explicit visual requests, or Board UI/rendering changes.
