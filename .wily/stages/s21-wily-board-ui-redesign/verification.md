# Verification

Verify the redesigned Board UI with:

- Board DB and web route tests.
- Dashboard rendering for shared repos, personal repos, active sessions, stale sessions, blockers, awaiting-push items, and unclaimed ready work.
- Repo detail rendering that keeps durable status distinct from live overlay status.
- Desktop and mobile browser screenshots for the main dashboard and at least one repo detail page.
- A smoke check that no write action pushes directly to a target repository.
