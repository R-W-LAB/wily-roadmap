# Phase 21-6: Preferences, command palette, theme, and responsive polish

## Purpose

Finish expected dashboard controls and polish the experience before cutover.

## Acceptance

- Header repo switcher uses cmdk and opens from the button plus `Cmd-K`/`Ctrl-K`.
- Theme toggle uses `next-themes` without first-paint flicker.
- Rail collapsed/expanded state persists per device and respects width-aware defaults until the user toggles.
- Korean relative time labels render for live activity.
- Sonner toasts report live connection state and copy actions only, not data mutations.
- Keyboard focus order is logical and focus rings are visible.
- Desktop and mobile screenshots show no incoherent overlap or clipped controls.
