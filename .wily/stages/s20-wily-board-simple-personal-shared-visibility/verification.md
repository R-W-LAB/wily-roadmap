# Verification

Run Board DB and web route tests. Verify:

- `Julirsia` sees shared repos only.
- `airmang` sees shared repos plus personal repos assigned to `airmang`.
- Direct URL access to personal repos is denied to non-owners.
