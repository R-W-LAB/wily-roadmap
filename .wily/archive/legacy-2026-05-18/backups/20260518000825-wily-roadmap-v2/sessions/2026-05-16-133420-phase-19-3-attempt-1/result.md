# Result

Implemented the Board Attention risk view.

- Dashboard now receives `risk_items` from `list_risk_items`.
- Added compact `Attention` section with severity, repo, stage/phase, title, reason text, PR chip support, and repo-detail anchors.
- Clean boards render a quiet `No attention items.` empty state.
