# Result

Implemented Board freshness-aware live presence rendering.

- Stale live sessions no longer appear in `Active right now`.
- Stale live sessions no longer block a ready/pending phase from `Up next`.
- Repo detail still shows stale local activity with a `last seen` label for operator awareness.
- Live chips now receive freshness-specific CSS classes for fresh, recent, and stale states.
