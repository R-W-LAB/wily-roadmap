# Verification

Run phase-scoped Board and Wily CLI tests.

For end-to-end verification, confirm that `$wily-start`, `$wily-block`, and `$wily-complete` can update Board live state before push, and that a GitHub/durable sync clears matching provisional overlays.
