stage_id: "s28"
schema: "wily-roadmap-v2"
phases:
  - id: "28-1"
    title: "Remove legacy Jinja templates"
    path: "stages/s28-board-readonly-cutover/phases/28-1-remove-legacy-templates"
    status: "done"
    depends_on: []
    parallel_group: "28"
    owner: "codex"
    task: "delete app/web/templates/{board,repo_detail,_phase_row,base,_toast}.html and any unused static CSS once the Next.js frontend is the only renderer; ensure no template route still references them"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"
    current_session: "sessions/2026-05-18-114043-phase-s28-28-1-attempt-1"

  - id: "28-2"
    title: "Remove write actions and PR mutator"
    path: "stages/s28-board-readonly-cutover/phases/28-2-remove-actions"
    status: "done"
    depends_on: ["28-1"]
    parallel_group: "28"
    owner: "codex"
    task: "delete app/actions/{toggle_status,pr_writer}.py and any FastAPI router include or import that still wires phase status mutation or PR creation from the Board UI surface"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"
    current_session: "sessions/2026-05-18-120246-phase-s28-28-2-attempt-1"

  - id: "28-3"
    title: "Route cutover to Next.js only"
    path: "stages/s28-board-readonly-cutover/phases/28-3-route-cutover"
    status: "done"
    depends_on: ["28-2"]
    parallel_group: "28"
    owner: "codex"
    task: "replace the Jinja `/` route in app/web/routes.py with either deletion or a redirect to the Next.js frontend; confirm reverse proxy routes /api/* to FastAPI and everything else to Next.js without dual surfaces"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"
    current_session: "sessions/2026-05-18-120314-phase-s28-28-3-attempt-1"

  - id: "28-4"
    title: "Read-only invariant regression tests"
    path: "stages/s28-board-readonly-cutover/phases/28-4-readonly-regression-tests"
    status: "done"
    depends_on: ["28-3"]
    parallel_group: "28"
    owner: "codex"
    task: "add a backend test that enumerates every FastAPI route and asserts there is no POST/PUT/DELETE handler that mutates roadmap state (live event ingest endpoints are allowed); add a frontend smoke test that loads / and /repos/owner/name and asserts no status select / Open PR button appears"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"
    current_session: "sessions/2026-05-18-120345-phase-s28-28-4-attempt-1"
