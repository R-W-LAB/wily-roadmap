stage_id: "s30"
schema: "wily-roadmap-v2"
phases:
  - id: "30-1"
    title: "react-flow dagre layout"
    path: "stages/s30-board-dag-components-mobile/phases/30-1-dagre-layout"
    status: "done"
    depends_on: []
    parallel_group: "30"
    owner: "codex"
    task: "replace the current index*250 zigzag positioning in stage-map.tsx with a proper dagre layout pass so stage nodes and dependency edges respect the topological structure; preserve Done blob behavior"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"

  - id: "30-2"
    title: "Headline strip + Tremor progress"
    path: "stages/s30-board-dag-components-mobile/phases/30-2-headline-tremor"
    status: "done"
    depends_on: []
    parallel_group: "30"
    owner: "codex"
    task: "add a Headline component to the repo workspace page rendering '{repo} · {done}/{total} stages ━━━ {left} left' with Tremor ProgressBar; show user's active phase summary below"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"

  - id: "30-3"
    title: "Attention section component"
    path: "stages/s30-board-dag-components-mobile/phases/30-3-attention-section"
    status: "done"
    depends_on: []
    parallel_group: "30"
    owner: "codex"
    task: "add an Attention component grouping blocked + needs_review items in the current repo as a shadcn Alert list; render only when non-empty"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"

  - id: "30-4"
    title: "Mobile fallback (DAG vertical list + bottom sheet rail)"
    path: "stages/s30-board-dag-components-mobile/phases/30-4-mobile-fallback"
    status: "done"
    depends_on: ["30-1"]
    parallel_group: "30"
    owner: "codex"
    task: "below 600px viewport, render stages as a vertical list with status colors and live chips instead of the react-flow canvas; replace the local desk rail with a sticky top badge that opens a shadcn Sheet side=bottom containing the full rail content"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"

  - id: "30-5"
    title: "Repo switcher and pin polish"
    path: "stages/s30-board-dag-components-mobile/phases/30-5-repo-switcher-polish"
    status: "done"
    depends_on: []
    parallel_group: "30"
    owner: "codex"
    task: "sort cmdk repo switcher list with recently-visited first then alphabetical, split into Shared and Personal groups, mark pinned repos with star; reflect pin status in the Hub repo list ordering so pinned repos appear at the top of their visibility group"
    runner: ""
    preferred: "manual"
    autonomy_mode: "goal_scoped"
